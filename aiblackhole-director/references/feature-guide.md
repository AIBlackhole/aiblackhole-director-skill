# Feature Guide

Use this reference when explaining how to use the online director.

## Online Entry

Open:

https://aiblackhole.net/panorama-viewer

## Main Modes

- Panorama image mode: use for 360/720 panorama images. The user can rotate the camera view and export the current view.
- Flat image mode: use for ordinary images. The director tools work in a fixed 2D-like stage.

## Main Features

- Upload panorama or normal images.
- Add characters.
- Add cubes and spheres.
- Add occlusion planes, circles, boxes, and spheres.
- Move, rotate, and scale selected objects.
- Batch-select multiple objects and transform them as one group.
- Lock or unlock objects.
- Hide or show the right director panel.
- Switch Chinese/English UI.
- Save the current view as a PNG screenshot.

## Character Controls

Characters support:

- pose presets
- body color and size
- XYZ position
- XYZ rotation
- arm and leg controls
- head turn, pitch, and side tilt
- waist turn, bend, and side bend
- palm and foot rotation
- optional skeleton visibility

When explaining body controls, use plain language first. Mention exact control names only when the user is trying to adjust a specific body part.

## Shortcuts

- `H`: hide/show the director panel
- `F`: toggle full screen
- `R`: reset camera
- `S`: save the current view

## Screenshot Behavior

Screenshots export the current camera view. Occluders are used to hide character/body/object portions as staging tools; they should not permanently alter the original uploaded image.

## Suggested User Workflow

1. Open the online director.
2. Upload a panorama or normal image.
3. Add characters and objects.
4. Use occluders to simulate partial blocking.
5. Adjust camera, pose, and transforms.
6. Hide the side panel if it blocks the view.
7. Save the final view as PNG.
