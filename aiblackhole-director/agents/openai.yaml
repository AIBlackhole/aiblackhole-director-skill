interface:
  display_name: "AI Blackhole Director"
  short_description: "Create local director sessions and import local panorama images."
  default_prompt: "Use $aiblackhole-director to open a local image in a local panorama director session."
