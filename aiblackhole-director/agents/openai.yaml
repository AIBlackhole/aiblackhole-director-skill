interface:
  display_name: "AI Blackhole Director"
  short_description: "Open local director sessions and import panorama images."
  default_prompt: "Use $aiblackhole-director to open the local director and load a local panorama image."
